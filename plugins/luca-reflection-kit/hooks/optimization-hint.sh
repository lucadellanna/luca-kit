#!/bin/bash
# luca-reflection-kit:optimization-hint:v4
echo "When you finish this response, if it involved 8+ tool calls, draft one 'Optimization hint' (memory-worthy pattern / skill to edit or improve). Before printing: ask whether it names something specific and actionable — if generic or obvious, skip. One sentence. Skip if exploratory or one-off. Skip if you already captured this pattern in this session (written to memory, code-review checklist, or a CLAUDE.md rule)."
